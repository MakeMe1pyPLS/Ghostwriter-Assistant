You are a senior SaaS architect, React + TypeScript frontend engineer, and FastAPI backend engineer.

Refactor and stabilize my existing application:

"ChainInsideIQ — AI Dashboard Generator Platform"

IMPORTANT:
Do NOT rebuild the app.
CLEAN, SIMPLIFY, and FIX what already exists.

PRIMARY GOAL:
Make the app usable, fast, and stable for launch.

--------------------------------------------------

CRITICAL PRIORITIES (IN ORDER)

1) FIX the "Generate Dashboard" button (currently broken)
2) REMOVE unnecessary complexity from the wizard
3) SIMPLIFY sector selection and business structure flow
4) CLEAN the UI and navigation
5) ENSURE the app works end-to-end without errors

--------------------------------------------------

PART 1 — FIX GENERATOR BUTTON (TOP PRIORITY)

The "Generate Dashboard" button is currently throwing an error.

Tasks:

1) Trace the full flow:
   - frontend button click
   - state being passed
   - API request
   - backend endpoint
   - response handling

2) Identify why it fails:
   - missing state?
   - undefined sectors?
   - incorrect payload?
   - API route mismatch?

3) Fix it so that:
   - button always works when required inputs are present
   - errors are handled gracefully
   - loading state is shown during generation

4) Add validation BEFORE execution:

Frontend check:

- businessType must exist
- sectors must be selected

If not:
Show message:
"Please complete setup before generating your dashboard."

--------------------------------------------------

PART 2 — SIMPLIFY THE WIZARD (REMOVE CLUTTER)

Reduce the wizard to ONLY 3 steps:

STEP 1 — Business Type

Options:
- Single Business
- Partnered Business
- Unified Supply Chain

Short descriptions only (1 line max)

--------------------------------------------------

STEP 2 — Sector Selection (SIMPLIFIED)

Use clean dropdown(s) or selectors.

Rules:

Single Business:
- Select 1 sector

Partnered Business:
- Select Sector A
- Select Sector B

Unified Supply Chain:
- Select up to 3 sectors

Available sectors ONLY:
- E-commerce
- Logistics
- Manufacturing

REMOVE:
- Custom sector option
- advanced configurations

--------------------------------------------------

STEP 3 — Generate

Single button:

"Generate My Dashboard"

--------------------------------------------------

PART 3 — REMOVE UNNECESSARY FEATURES (TEMPORARILY)

Completely REMOVE or DISABLE from UI:

- Data Sharing toggle
- Request to Share Data
- Shared Data Center
- Hub communication system
- Advanced dataset selection
- Any complex multi-step configurations

These can be reintroduced AFTER launch.

--------------------------------------------------

PART 4 — SIMPLIFY GENERATOR PAYLOAD

Frontend should send ONLY:

{
  "business_type": "single | partnered | unified",
  "sectors": ["ecommerce", "logistics"]
}

No extra data.

Backend must accept this payload and generate dashboards accordingly.

--------------------------------------------------

PART 5 — CLEAN NAVIGATION

Navbar should ONLY include:

- Dashboard
- Generate
- Settings
- Pricing

REMOVE or HIDE:
- Hub
- Shared Data
- experimental features

--------------------------------------------------

PART 6 — ADD LOADING + ERROR STATES

When user clicks generate:

- show loading spinner or message:
  "Generating your dashboard..."

If error occurs:
- show user-friendly message:
  "Something went wrong. Please try again."

--------------------------------------------------

PART 7 — DASHBOARD GENERATION LOGIC

Keep it simple:

If sector = E-commerce:
- show sales KPIs

If sector = Logistics:
- show fulfillment KPIs

If sector = Manufacturing:
- show production KPIs

If multiple sectors:
- combine dashboards logically
- no advanced cross-sector logic required yet

--------------------------------------------------

PART 8 — SETTINGS CLEANUP

Settings page should ONLY include:

- Business Type
- Selected Sectors

REMOVE:
- data sharing settings
- hub toggles
- advanced configurations

--------------------------------------------------

PART 9 — TESTING REQUIREMENTS

Ensure:

1) User can select business type
2) User can select sectors
3) Generate button works EVERY time
4) Dashboard loads correctly
5) No console errors
6) UI is responsive on mobile + desktop

--------------------------------------------------

PART 10 — DELIVERABLES

After refactor:

1) List files modified
2) Explain what caused the generator issue
3) Show how it was fixed
4) Confirm wizard is now simplified
5) Confirm removed features are safely disabled (not breaking app)