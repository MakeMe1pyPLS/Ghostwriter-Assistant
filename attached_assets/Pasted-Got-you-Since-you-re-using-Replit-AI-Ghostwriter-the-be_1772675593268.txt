Got you. Since you’re using Replit AI / Ghostwriter, the best thing is to give it a single master prompt that tells it exactly what to build, how to structure the code, and what libraries to use. This prevents the broken code issues you kept getting.

This prompt will generate a working SaaS dashboard builder demo that you can screen record for Fiverr/Upwork and eventually turn into part of ChainInsideIQ.

Just copy and paste this entire prompt into Replit AI.

⸻

MASTER REPLIT BUILD PROMPT (Copy & Paste)

Build a production-quality interactive SaaS demo called “ChainInsideIQ — AI Supply Chain Dashboard Builder”.

The goal is to create a clean, enterprise-style dashboard builder that I can click through, drag widgets, and screen-record for Fiverr/Upwork clients.

The UI must look premium and modern, not like a tutorial project.

⸻

TECH STACK (use exactly)

Frontend
• React
• Vite
• TypeScript

UI
• TailwindCSS
• shadcn/ui components

Dashboard Layout
• react-grid-layout (drag and resize widgets)

Charts
• Recharts

Routing
• react-router-dom

Backend
• FastAPI (Python)

Data Storage
• In-memory JSON demo data (no database needed)

Export Feature
• Fully working Export CSV
• Stub buttons for Export Excel and Export PDF

The project should have two main folders:

/frontend
/backend

⸻

DESIGN STYLE

Use a clean corporate SaaS style.

Color palette:

Background
#F4F7FA

Cards
#FFFFFF

Borders
#E3E8EF

Primary teal
#0F766E

Accent teal
#14B8A6

Text
Headings: #0F172A
Secondary: #64748B
Muted: #94A3B8

Typography
Inter or system UI font

Design rules

• Soft shadows
• Rounded corners
• Clean spacing
• Subtle hover effects
• Smooth transitions
• Professional SaaS appearance

The UI should look similar to Stripe, Notion dashboards, or modern BI tools.

⸻

APP PAGES

Create these routes:

/builder
/dashboard
/insights
/hub
/connectors
/exports

⸻

BUILDER PAGE

Main feature: drag and drop dashboard builder

Layout sections:

Left Sidebar Navigation

Builder
Dashboard
AI Insights
Hub
Connectors
Exports
Settings

Top Bar

Sector switch dropdown

E-commerce
Logistics
Manufacturing
Unified

Date range selector
Refresh button

Main Canvas

Drag-and-drop dashboard grid using react-grid-layout.

Widgets must be:

Resizable
Draggable
Movable

Widget library panel on the right with buttons to add:

KPI Card
Trend Line Chart
Bar Chart
Donut Chart
Table
AI Insights
AI Chat

Layout must save to localStorage.

⸻

DASHBOARD VIEW

Route: /dashboard

This is a presentation mode version of the dashboard.

No dragging.

Just clean KPI cards and charts updating from demo data.

⸻

AI ANALYST CHAT

Create a chat UI similar to ChatGPT.

Features

Message bubbles
Scrollable history
Input field
Send button

Backend endpoint

POST /api/chat

Return a structured response with:

What changed
Why it matters
Top 3 recommended actions
14-day forecast explanation

For now this can be rule-based demo responses.

But structure the code so OpenAI API can be added later.

⸻

BRIDGE KPI ENGINE (IMPORTANT)

Create special cross-industry KPIs that act as the core feature.

Perfect Order Rate
Cash-to-Cash Cycle Time
Available-to-Promise Accuracy
Bullwhip Effect Index

Display these as KPI cards.

They represent analytics across:

E-commerce
Logistics
Manufacturing

These KPIs are the core differentiation of the platform.

⸻

FORECAST PANEL

Show a 14-day forecast chart.

Include:

Trend line
Confidence band
Forecast confidence badge

Demo data is acceptable.

⸻

COMMUNICATION HUB

Route: /hub

Allow users to post updates between sectors.

Example messages

“E-commerce demand spike detected”
“Warehouse delay reported”
“Manufacturing capacity increase scheduled”

Features

Post message
Choose sector
Optional KPI tag

Messages show in a feed grouped by sector.

Store messages in memory on the backend.

Endpoints

GET /api/hub
POST /api/hub

⸻

CONNECTORS PAGE

Route: /connectors

Show integration tiles for:

Excel
Google Sheets
Power BI
Tableau
SQL Database

Each tile has a Connect button that opens a modal.

This is a mock integration UI.

Purpose is to show the platform works with existing tools.

⸻

EXPORT PAGE

Route: /exports

Buttons:

Export CSV (fully working)
Export Excel (placeholder)
Export PDF (placeholder)

CSV should download demo metrics for the selected sector.

⸻

DEMO DATA

Create realistic demo metrics.

Examples:

Revenue
Orders
Inventory
Defects
On-time delivery
Production capacity
Shipping delays

Endpoints

GET /api/metrics
GET /api/timeseries
GET /api/breakdowns
GET /api/forecast
GET /api/insights
POST /api/chat
GET /api/hub
POST /api/hub

⸻

POLISH REQUIREMENTS

Add the following UX features:

Loading skeletons when data loads
Toast notifications for exports and refresh
Empty state UI for dashboards without widgets
Demo mode badge
Last refreshed timestamp
Theme toggle button (light only required)

⸻

IMPORTANT UI REQUIREMENTS

The UI must look like a real SaaS product.

Avoid generic tutorial styling.

Use:

Rounded cards
Clean KPI widgets
Professional spacing
Consistent typography

The dashboard must look good enough for portfolio screenshots.

⸻

DELIVERABLES

Generate a full project that:

Runs in Replit
Has a single Run button
Requires no external credentials
Uses demo data
Has clean, commented code

Include a README with run instructions.

⸻

FINAL NOTE

The application must feel like a real product prototype, not a simple coding demo.

The goal is to produce something I can:

Show to clients
Screen-record for Fiverr
Expand into a full SaaS later.

⸻

Why this prompt works for you

This will generate a real dashboard builder SaaS demo, which gives you:

✔ A portfolio project
✔ A Fiverr demo
✔ A future SaaS foundation
✔ A drag-and-drop dashboard system

This aligns perfectly with the platform you’re building.

⸻

Important strategic note for you

This approach lets you monetize before the SaaS is finished, which is exactly what you were trying to do.

You can sell:

• Custom dashboards
• AI insights dashboards
• Supply chain analytics dashboards
• Excel / BI dashboards

while still building ChainInsideIQ.

⸻

If you want, I can also show you something extremely powerful next:

How to turn this into a $1,500+ Fiverr/Upwork service using the exact same tool you’re building.