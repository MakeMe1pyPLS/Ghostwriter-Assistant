You are a senior SaaS architect, React + TypeScript frontend engineer, and FastAPI backend engineer.

Enhance my existing application:

"ChainInsideIQ — AI Dashboard Generator Platform"

IMPORTANT
Do NOT rebuild the application.
Refactor and extend the current codebase in-place.

The platform already has:
- dashboard builder
- AI supply chain analyst
- export system
- integrations scaffolding
- widget library
- dashboard generator wizard

We are FINALIZING the product for launch.

PRIMARY GOAL
Add a proper business structure system so the software supports:

1) Single Business
2) Partnered Business (2 Sectors / Co-Op)
3) Unified Supply Chain (3 Sectors)

The platform must behave differently depending on the structure selected.

The system must remain simple for single businesses and more powerful for partnered/unified companies.

In addition, add cross-data viewing capability so sectors can view shared data from each other when data sharing is enabled and approved.

This should make it possible for users to understand that:
- sectors can remain independent
- sectors can optionally share data
- sectors can optionally view shared data from each other
- partnered and unified businesses can collaborate more effectively

--------------------------------------------------

PART 1 — ADD BUSINESS STRUCTURE WIZARD STEP

In the dashboard generator wizard (Generate For Me flow), add a new step:

Step Title:
Business Structure

Options:

1️⃣ Single Business
Description:
One company operating in one sector.

Examples:
• E-commerce brand
• Logistics warehouse
• Manufacturing company

2️⃣ Partnered Business (2 Sectors)
Description:
Two sectors working together or sharing operational data.

Examples:
• E-commerce + Logistics
• Manufacturer + Logistics
• Brand + 3PL partner

This mode supports co-op businesses and partnerships.

3️⃣ Unified Supply Chain (3 Sectors)
Description:
Three sectors connected in a full supply chain.

Example:
Manufacturer → E-commerce → Logistics

This unlocks cross-sector dashboards, communication, data sharing, and supply chain intelligence.

--------------------------------------------------

PART 2 — SECTOR SELECTION LOGIC

After selecting business structure, the wizard should request sectors.

IF Single Business:
Allow selecting ONE sector:
• E-commerce
• Logistics
• Manufacturing
• Custom

IF Partnered Business (2 sectors):
Allow selecting TWO sectors.

Examples:
• E-commerce + Logistics
• Manufacturing + Logistics
• E-commerce + Manufacturing

IF Unified Supply Chain:
Allow selecting up to THREE sectors.

Maximum sectors allowed: 3

--------------------------------------------------

PART 3 — DATA SHARING SETTINGS

After sector selection, ask:

"Do you want to allow sectors to share operational data?"

Options:

✔ Enable Data Sharing
❌ Keep Sectors Independent

Explain in helper text:

Enabling data sharing allows sectors to exchange metrics, demand signals, fulfillment data, inventory visibility, and performance indicators.

This unlocks:
- cross-sector insights
- cross-sector AI intelligence
- optional shared dashboards
- cross-data viewing between approved sectors

--------------------------------------------------

PART 4 — REQUEST TO SHARE DATA FEATURE

Add a new feature called:

"Request to Share Data with Sector"

Create a button visible in:

• Sector settings
• Dashboard settings
• Data management area
• Shared Data Center (if created)
• Optional hub-related controls if the business type supports it

When clicked:

Open a modal where the user can:

Select sector to share with

Select dataset to share

Example datasets:
• Orders
• Demand
• Inventory
• Fulfillment
• Shipping performance
• Production output
• Custom dataset

Add optional message field.

Example:
"Sharing order demand data so logistics can forecast fulfillment volume."

Add submit button.

--------------------------------------------------

PART 5 — REQUEST STATUS SYSTEM

Add basic request status tracking:

Statuses:
- Pending
- Approved
- Rejected

Display request status inside a new panel:

"Shared Data Requests"

For launch version, this can be stored in local state or simple backend records.

Full enterprise permission systems are not required yet.

--------------------------------------------------

PART 6 — CROSS-DATA VIEWING CAPABILITY

Add a new capability called:

"View Shared Sector Data"

This should allow one sector to view data that another sector has chosen to share, only when:

- data sharing is enabled
- a request has been approved
- the sector relationship allows it

Examples:
- E-commerce can view logistics fulfillment metrics if logistics approved sharing
- Logistics can view ecommerce demand/order trends if ecommerce approved sharing
- Manufacturing can view logistics shipment performance if shared
- Unified supply chain view can display approved cross-sector data together

IMPORTANT:
This must be optional and permission-based.

For launch, it is acceptable if the feature supports:
- basic approval-based visibility
- read-only cross-data viewing
- no advanced field-level permissions yet

--------------------------------------------------

PART 7 — SHARED DATA CENTER / HUB INTEGRATION

Implement cross-data viewing in one of these ways:

OPTION A:
Create a dedicated section called:
"Shared Data Center"

This section can show:
- sectors you are connected to
- what data has been shared with you
- what data you are sharing outward
- request status
- available shared datasets
- a "View Shared Data" action

OPTION B:
Integrate this into the existing Hub if the business structure supports it

If using the Hub:
- create a tab or subsection for shared data
- do not overload the main feed
- keep shared data discoverable and organized

Behavior by business structure:

Single Business
- Shared Data Center hidden by default
- no cross-data viewing unless the user later adds collaboration settings

Partnered Business
- Shared Data Center or hub section becomes available
- supports cross-data viewing between the two sectors if sharing is approved

Unified Supply Chain
- Shared Data Center or hub section is available
- can show shared data across all linked sectors
- can surface bridge metrics and cross-sector summaries

--------------------------------------------------

PART 8 — HUB LOGIC BASED ON BUSINESS STRUCTURE

The operational hub should behave differently depending on the business structure.

Single Business
- Hub optional
- Only internal alerts or AI insights
- No cross-sector collaboration required

Partnered Business
- Hub optional but recommended
- Used for sector communication, status updates, and shared metrics
- Shared data panel or view should be available if sharing is enabled

Unified Supply Chain
- Hub enabled
- Supports:
  • sector alerts
  • shared KPIs
  • AI insights
  • operational updates
  • shared data visibility
  • cross-sector coordination

The UI should adapt accordingly.

--------------------------------------------------

PART 9 — DASHBOARD GENERATION LOGIC

Update dashboard generator logic to adapt to structure.

Single Business
- Generate one sector dashboard
- No shared data sections by default

Partnered Business
- Generate two dashboards or a split layout with both sectors
- If sharing is enabled, include a shared-data summary section
- If applicable, allow one sector to view approved data from the other

Unified Supply Chain
- Generate supply chain overview dashboard including:
  • sector summaries
  • bridge KPIs
  • cross-sector metrics
  • shared data summaries where approved
  • communication/hub widgets if enabled

--------------------------------------------------

PART 10 — AI CONTEXT IMPROVEMENT

The AI Supply Chain Analyst must understand the business structure.

Single Business
- AI analyzes only one sector

Partnered Business
- AI can analyze relationships between the two sectors
- AI can reference approved shared data

Unified Supply Chain
- AI can analyze the entire supply chain and cross-sector dependencies
- AI can reference bridge KPIs and approved shared data

--------------------------------------------------

PART 11 — SETTINGS PAGE UPDATE

Add a new settings section:

"Business Structure Settings"

Display:
- Business Type:
  Single / Partnered / Unified
- Connected Sectors
- Data Sharing Status
- Hub Enabled / Disabled
- Shared Data Visibility Status

Allow user to modify these settings later.

If sharing is enabled, allow user to:
- see what sectors can view their shared data
- revoke shared visibility
- manage requests
- review approved relationships

--------------------------------------------------

PART 12 — SUPPORT DOCUMENTATION UPDATE

Update support page to include sections explaining:

- Single Business Mode
- Partnered Business Mode
- Unified Supply Chain Mode
- Data Sharing
- Request to Share Data
- Cross-Data Viewing
- Hub Communication
- Shared Data Center (if implemented)

Make sure the documentation clearly explains that:
- sectors can keep data private
- sectors can optionally share data
- approved sectors can view shared data
- more advanced collaboration can be expanded in future updates

--------------------------------------------------

PART 13 — UI DESIGN REQUIREMENTS

Keep design consistent with current UI.

Use:
- Tailwind
- existing UI components
- clean spacing
- responsive layouts
- clear status labels
- premium but simple controls

Ensure functionality works on:
- Desktop
- Tablet
- Mobile

--------------------------------------------------

PART 14 — DELIVERABLES

1) Inspect the current repo structure
2) Identify files related to:
   - wizard and business structure logic
   - sector logic
   - shared data requests
   - cross-data viewing
   - hub/shared data section
   - settings page
   - support page
3) Add the Business Structure step
4) Implement sector selection rules
5) Implement data sharing request feature
6) Implement request status tracking
7) Implement cross-data viewing capability
8) Add Shared Data Center or shared data hub section
9) Update hub behavior logic
10) Update dashboard generation logic
11) Update AI context
12) Update support page documentation

After implementation explain:

• files modified
• new components created
• new backend endpoints if applicable
• how the system now supports:
  - single businesses
  - partnered/co-op businesses
  - unified supply chains
  - optional cross-data viewing between approved sectors